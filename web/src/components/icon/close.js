import React from 'react';
import { IoMdClose } from 'react-icons/io';

const CloseIcon = () => <IoMdClose style={{fill: 'black', color: 'black', fontSize: '2rem', cursor: 'pointer'}} />;

export default CloseIcon;